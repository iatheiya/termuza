name: termuza

on:
  push:
    branches: [ master ]
    paths:
      - '**/*.kt'
      - '**/*.java'
      - '**/*.xml'
      - '**/*.gradle'
      - '**/*.gradle.kts'
      - 'gradle/**'
      - 'app/**'
  pull_request:
    paths:
      - 'app/**'
      - 'gradle/**'

jobs:
  build-release-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      BUILD_TOOLS_VERSION: '33.0.2'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 33
          build-tools: ${{ env.BUILD_TOOLS_VERSION }}
          components: |
            build-tools;${{ env.BUILD_TOOLS_VERSION }}
            platforms;android-33

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Prepare signing keystore
        id: keystore
        run: |
          set -eux
          # if user provided keystore base64 in secrets — decode it, else generate temporary keystore
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "Using keystore from secrets"
            echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > release.keystore
            KEYSTORE_PASSWORD="${{ secrets.ANDROID_KEYSTORE_PASSWORD }}"
            KEY_ALIAS="${{ secrets.ANDROID_KEY_ALIAS }}"
            KEY_PASSWORD="${{ secrets.ANDROID_KEY_PASSWORD }}"
          else
            echo "No keystore secret found — generating temporary keystore for CI"
            KEYSTORE_PASSWORD=android
            KEY_ALIAS=ci_release
            KEY_PASSWORD=android
            keytool -genkeypair -v \
              -keystore release.keystore \
              -alias "$KEY_ALIAS" \
              -storepass "$KEYSTORE_PASSWORD" \
              -keypass "$KEY_PASSWORD" \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=CI, OU=CI, O=CI, L=CI, ST=CI, C=US"
          fi
          # export outputs for later steps
          echo "keystore-path=release.keystore" >> $GITHUB_OUTPUT
          echo "keystore-password=$KEYSTORE_PASSWORD" >> $GITHUB_OUTPUT
          echo "key-alias=$KEY_ALIAS" >> $GITHUB_OUTPUT
          echo "key-password=$KEY_PASSWORD" >> $GITHUB_OUTPUT

      - name: Assemble release APK (unsigned)
        run: |
          set -eux
          ./gradlew assembleRelease --no-daemon

      - name: Find unsigned release APK
        id: find_apk
        run: |
          set -eux
          apk=$(find . -type f -path "*/build/outputs/apk/*/release/*-unsigned.apk" | head -n1 || true)
          if [ -z "$apk" ]; then
            # fallback to any release apk
            apk=$(find . -type f -path "*/build/outputs/apk/*/release/*.apk" | grep -v '\-aligned' | head -n1 || true)
          fi
          if [ -z "$apk" ]; then
            echo "No release APK found" >&2
            exit 1
          fi
          echo "apk=$apk" >> $GITHUB_OUTPUT
          echo "Found unsigned APK: $apk"

      - name: Sign APK with apksigner
        id: sign
        run: |
          set -eux
          unsigned="${{ steps.find_apk.outputs.apk }}"
          out_signed="${unsigned%.*}-release-signed.apk"

          # try configured build-tools location, else pick latest installed build-tools
          bt="$ANDROID_SDK_ROOT/build-tools/${{ env.BUILD_TOOLS_VERSION }}"
          if [ ! -x "$bt/apksigner" ]; then
            bt=$(ls -d $ANDROID_SDK_ROOT/build-tools/* | sort -V | tail -n1)
          fi
          echo "Using apksigner from: $bt"

          "$bt/apksigner" sign \
            --ks "${{ steps.keystore.outputs.keystore-path }}" \
            --ks-pass "pass:${{ steps.keystore.outputs.keystore-password }}" \
            --key-pass "pass:${{ steps.keystore.outputs.key-password }}" \
            --out "$out_signed" "$unsigned"

          "$bt/apksigner" verify "$out_signed"
          echo "signed_apk=$out_signed" >> $GITHUB_OUTPUT
          echo "Signed APK: $out_signed"

      - name: Collect mapping / metadata (if present)
        run: |
          set -eux
          # if Proguard / R8 generated mapping file, collect it
          mapping=$(find . -type f -path "*/build/outputs/mapping/*/mapping.txt" | head -n1 || true)
          if [ -n "$mapping" ]; then
            echo "Found mapping: $mapping"
            mkdir -p release_artifacts
            cp "$mapping" release_artifacts/mapping.txt
          else
            echo "No mapping file found"
          fi

      - name: Upload artifacts — signed + unsigned + mapping (if any)
        uses: actions/upload-artifact@v4
        with:
          name: termuza-release-apks
          path: |
            ${{ steps.sign.outputs.signed_apk }}
            ${{ steps.find_apk.outputs.apk }}
            release_artifacts/**
